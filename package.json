{
  "name": "mern-qa",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npm run test:unit && npm run test:api && npm run test:ui",
    "test:unit": "jest tests/unit --runInBand",
    "test:api": "jest tests/api --runInBand",
    "test:ui": "mocha tests/ui --timeout 60000",
    "start:ci": "node server.js",
    "lint": "npx eslint .",
    "ci": "npm ci && nohup npm run start:ci > server.log 2>&1 & npx wait-on http://localhost:3000 && npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "xss": "^1.0.15"
  },
  "devDependencies": {
    "chai": "^6.0.1",
    "chromedriver": "^140.0.3",
    "eslint": "^9.36.0",
    "jest": "^30.1.3",
    "mocha": "^11.7.2",
    "nodemon": "^3.1.10",
    "selenium-webdriver": "^4.35.0",
    "supertest": "^7.1.4",
    "wait-on": "^9.0.1"
  }
}
